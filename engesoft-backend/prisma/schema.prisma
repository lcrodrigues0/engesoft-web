generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum BaseType {
  GUEST
  CONTRIBUTOR
}

model User {
  id            String      @id @default(uuid())
  name          String      @unique
  email         String      @unique
  password      String
  refreshToken  String?
  baseType      BaseType    @default(GUEST)
  createdAt     DateTime    @default(now())
}

